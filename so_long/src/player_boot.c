#include "so_long.h"
#include "types.h"

t_cycle	*ft_load_cycle(char model, int entity, t_dir dir, void *mlx)
{
	t_cycle	*new_cycle;

	new_cycle = malloc(sizeof(t_cycle));
	if (!new_cycle)
		return (NULL);
}
